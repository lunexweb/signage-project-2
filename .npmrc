legacy-peer-deps=true
omit=optional

# Increase resilience for Vercel installs (handles transient 503s)
fetch-retries=5
fetch-retry-factor=2
fetch-retry-maxtimeout=600000
fetch-retry-mintimeout=20000
timeout=600000
registry=https://registry.npmjs.org/
strict-ssl=true